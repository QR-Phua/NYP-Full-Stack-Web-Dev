$(document).ready(function () {

    function jump_To_Page(item_ID){

        var index_Value = id.split("_");

        var order_ID = index_Value[1];
        var product_Name = index[2];

        window.location.replace("/order/custorder_Info/"+ order_ID+"/"+product_Name);
    }

});